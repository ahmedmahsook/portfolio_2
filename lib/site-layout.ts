/** Shared layout classes — full viewport width on mobile, capped only at lg+ */
export const siteShell = "site-shell w-full"
export const siteShellNarrow = "site-shell site-shell--narrow w-full"
export const siteDivider = "section-divider site-shell w-full"

/** Hero-only: no desktop container padding on mobile (see .hero-content-shell) */
export const heroContentShell = "hero-content-shell w-full"
